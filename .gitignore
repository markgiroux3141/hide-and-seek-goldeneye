.DS_Store
Thumbs.db
*.log
node_modules/
dist/
public/existing goldeneye levels/
.vscode/
*.swp
*.swo
*~

# Rust / WASM
target/
pkg/
Cargo.lock
*.wasm
# ...but the prebuilt CSG/cave WASM binaries are committed runtime deps (app imports them).
!src/core/csg/wasm/csg_wasm_bg.wasm
!src/core/cave/wasm/cave_wasm_bg.wasm

# Level editor quick-slots are local scratch; curated test bases get committed
# later under their own named files (not slotN).
native/levels/slot*.json

# Large regenerable procedural-anim debug dump (not a shared artifact).
anim_log.txt

# Third-party N64 decompilations kept as a local read-only reference for asset
# ripping + AI study. Each is its own upstream git repo under its own licence;
# re-clone with the commands in reference/README.md rather than committing them.
reference/

# Python bytecode from the asset tools.
__pycache__/
*.pyc
